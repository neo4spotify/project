LOAD CSV WITH HEADERS FROM 'file:///data/chunks.csv' AS row
RETURN count(row)

LOAD CSV WITH HEADERS FROM 'file:///data/concerts.csv' AS row
RETURN count(row)

LOAD CSV WITH HEADERS FROM 'file:///data/df_200x2iter.csv' AS row
RETURN count(row)


LOAD CSV WITH HEADERS FROM 'file:///data/name_spotify_dataset.csv' AS row
RETURN count(row)



ou

USING PERIODIC COMMIT 500
LOAD CSV WITH HEADERS FROM 'file:///data/chunks.csv'
 	AS ligne FIELDTERMINATOR ';'

	MERGE (c:chunks {user_id: ligne.user_id})
		SET c.artistname = ligne.artistname
		SET c.trackname = ligne.trackname
		SET c.playlistname = ligne.playlistname 




#### les deux derniers ne fonctionne pas. je regarderai tout a lheure si jarrive a les faire marcher

USING PERIODIC COMMIT 500
LOAD CSV WITH HEADERS FROM 'file:///data/concerts.csv'
 	AS ligne FIELDTERMINATOR ';'

	MERGE (cr:concerts {artist_id: ligne.artist_id})
		SET cr.artist_name = ligne.artist_name
		SET cr.genres = ligne.genres
		SET cr.popularity = ligne.popularity
		SET cr.related = ligne.related




USING PERIODIC COMMIT 500
LOAD CSV WITH HEADERS FROM 'file:///data/name_spotify_dataset.csv'
 	AS ligne FIELDTERMINATOR ';'

	MERGE (nsd:name_spotify_dataset {artist_id: ligne.artist_id})
		SET nsd.artist_name = ligne.artist_name
		SET nsd.genres = ligne.genres
		SET nsd.popularity = ligne.popularity
		SET nsd.related = ligne.related
